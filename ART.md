# 그림 만들기 (제미나이 연동)

> **이 문서는 옛 방식입니다.**
> 캐릭터는 `캐릭터/` 원본에서 `tools/build-chars.py` 로 만들고,
> 여기 적힌 `ch-*.jpg` 는 지금은 쓰지 않습니다.
> 배경(`bg-clouds.svg` 등)을 다시 만들 때만 참고하세요.

캐릭터와 배경을 제미나이로 만들어 게임에 적용하는 방법입니다.

## ⚠️ 먼저 알아둘 것 — 이미지 생성은 무료가 아닙니다

**제미나이 API 의 이미지 생성은 무료 등급에서 쓸 수 없습니다.**
키는 정상이어도 할당량이 0 이라 거부됩니다 (`limit: 0`).

| 방법 | 비용 | 손이 가는 정도 |
|---|---|---|
| **A. 결제 등록 후 스크립트** | 16장에 대략 1~2달러 | 명령 한 줄 |
| **B. 제미나이 웹앱에서 직접** | 무료 | 16번 복사·붙여넣기 |

B 로 하려면 [tools/prompts.md](tools/prompts.md) 를 보세요. 문구가 다 적혀 있습니다.
아래는 A 의 절차입니다.

---

## 1. API 키 발급 (처음 한 번만)

1. https://aistudio.google.com/apikey 접속 (구글 계정으로 로그인)
2. **Create API key** → 키 복사
3. 그 프로젝트에 **결제 수단을 등록**합니다 (이미지 생성은 유료 등급 전용)

## 2. 키 저장하기

프로젝트 폴더에서 아래 명령을 실행합니다. 따옴표 안에 키를 붙여넣으세요.

키를 복사해 둔 상태에서 아래를 그대로 붙여넣으세요. **고칠 부분이 없습니다.**

```bash
cd ~/Desktop/LYH/game && pbpaste | tr -d '\n\r ' > .gemini-key && echo "저장됨: $(wc -c < .gemini-key)자"
```

`저장됨: 39자` 가 나오면 정상입니다.

`.gemini-key` 는 `.gitignore` 에 등록되어 있어 **깃허브에 올라가지 않습니다.**
저장소가 공개라 이 부분이 특히 중요합니다.

## 3. 그림 만들기

```bash
node tools/gen-art.mjs
```

16장(배경 3 · 캐릭터 10 · 아이콘 3)이 `shared/art/` 에 저장됩니다.

| 명령 | 하는 일 |
|---|---|
| `node tools/gen-art.mjs` | 아직 없는 그림만 만듭니다 |
| `node tools/gen-art.mjs --list` | 무엇이 있고 없는지 봅니다 |
| `node tools/gen-art.mjs --only ch-cat --force` | 한 장만 다시 만듭니다 |
| `node tools/gen-art.mjs --force` | 전부 다시 만듭니다 |

## 4. 마음에 안 들면

[tools/art-manifest.mjs](tools/art-manifest.mjs) 의 문구를 고치고 그 장만 다시 만듭니다.

```bash
node tools/gen-art.mjs --only bg-sky --force
```

- `STYLE` 은 모든 그림에 공통으로 붙는 화풍 지시문입니다. 전체 분위기를 바꾸려면 여기를 고칩니다.
- 각 항목의 `prompt` 는 그 그림만의 내용입니다.

## 5. 올리기

```bash
# sw.js 의 VERSION 을 올린 뒤
git add -A
git commit -m "캐릭터·배경 그림 추가"
git push
```

---

## 그림이 없어도 게임은 돌아갑니다

**이게 이 구조의 핵심입니다.**

- **캐릭터·아이콘**: 이모지 위에 그림을 덮습니다. 파일이 없으면 `onerror` 로 스스로 사라져 이모지가 남습니다.
- **배경**: CSS 에서 그림을 그라데이션 위에 겹칩니다. 파일이 없으면 그라데이션이 그대로 보입니다.

그래서 16장 중 일부만 만들어도 되고, 마음에 드는 것만 남기고 나머지는 지워도 됩니다.

## 어디에 쓰이는지

| 그림 | 쓰이는 곳 |
|---|---|
| `bg-main.jpg` | 메인 선택화면 배경 |
| `bg-sky.jpg` | 스톱워치 · 룰렛 배경 |
| `bg-paper.jpg` | 사다리 배경 |
| `ch-*.png` | 사다리 캐릭터(8) · 룰렛 칸(10) · 참가자 목록 |
| `ic-trophy.png` / `ic-rock.png` | 사다리 당첨 · 꽝 |
| `ic-dino.png` | 예비 (메인 카드 썸네일용) |

## 비용

장당 대략 $0.03~0.07 입니다 (모델과 크기에 따라 다름). 16장이면 1~2달러 정도.
`--force` 로 전부 다시 만들면 그만큼 또 나가니, 고칠 때는 `--only` 로 필요한 장만 만드세요.
현재 요금은 https://ai.google.dev/pricing 에서 확인하세요.

## 형식 제약

API 가 내주는 형식은 **JPEG 뿐입니다.** PNG 는 거부됩니다.
그래서 투명 배경을 쓸 수 없는데, 캐릭터를 **둥근 파스텔 배지** 형태로 만들어
투명도 없이도 화면에 자연스럽게 얹히도록 했습니다.

## B. 결제 없이 직접 만들기

[tools/prompts.md](tools/prompts.md) 에 16장의 문구가 모두 적혀 있습니다.

1. [제미나이 웹앱](https://gemini.google.com) 에 문구를 붙여넣어 그림을 만듭니다
2. 받은 파일을 제자리에 넣습니다

```bash
bash tools/put-art.sh ch-cat ~/Downloads/Gemini_Generated_Image_abc.png
```

크기 조정과 jpg 변환은 알아서 처리합니다. 한 장만 넣어도 그 부분만 바뀝니다.


---

## 실제로 해보고 배운 것

무료 생성 서비스(`tools/gen-free.mjs`)로 여러 번 돌리며 알게 된 것들입니다.
문구를 고칠 때 참고하세요.

### 배경은 생성하지 않습니다

세 번 시도했지만 전부 실패해서 **CSS 그라데이션을 그대로 쓰기로 했습니다.**

- "귀여운 게임 일러스트" 같은 화풍을 쓰면 배경 한가운데에 캐릭터를 그려 넣습니다
- "가운데를 비워라" 라고 하면 가운데에 커다란 덩어리를 그립니다
- 무늬·질감으로만 설명하면 캐릭터는 사라지지만 단색 질감이 되어 밋밋합니다

매니페스트에 `skip: true` 로 남겨두었습니다. 다시 시도하려면 이름을 직접 지정하세요.

### 결과 아이콘도 생성하지 않습니다

트로피가 형체를 알아볼 수 없게 나왔습니다. 이모지 🏆 🪨 가 훨씬 또렷합니다.

### 캐릭터 문구에서 꼭 필요한 말

빠뜨리면 실제로 이런 일이 일어났습니다.

| 빠뜨린 말 | 실제로 나온 결과 |
|---|---|
| "한 마리만" | 토끼가 두 마리 |
| "정면" | 토끼가 옆모습 |
| "눈이 또렷하게" | 코알라에 눈이 없음 |
| "가운데" | 개구리가 아래로 치우침 |
| 동물의 생김새 | 곰이 "곰 귀 단 소녀"로 나옴 |

또 **"젤리 질감" 같은 표면 지시를 넣으면 동물의 특징이 뭉개집니다.**
그림 안에 원형 테두리를 그리라고 하면 그릇처럼 일그러지므로,
동그랗게 자르는 일은 CSS 에 맡기고 그림에는 얼굴만 채우게 합니다.

### 배경색은 동물 색과 대비되어야 합니다

**가장 중요한 규칙입니다.** 색이 비슷하면 이목구비가 배경에 묻혀 사라집니다.

| 실패 | 원인 |
|---|---|
| 분홍 돼지 + 분홍 배경 | 눈·코가 사라진 덩어리 |
| 회색 코알라 + 라벤더 배경 | 두 번 시도했으나 모두 눈이 없음 |
| 노란 병아리 + 노란 배경 | 옆모습에 눈 하나만 |
| 파란 문어 + 파란 배경 | 형체가 너무 작고 흐림 |

돼지 배경을 민트색으로 바꾸자 바로 해결됐습니다.

### 잘 안 그려지는 동물이 있습니다

문어 · 코알라 · 병아리는 여러 번 시도해도 안 됐습니다.
문구를 고치기보다 **게임에 나오는 동물을 바꾸는 편이 빠릅니다.**
강아지 · 돼지 · 펭귄으로 교체해서 해결했습니다.

### 마음에 안 드는 캐릭터가 있으면

그 파일 하나만 지우면 그 자리만 이모지로 돌아갑니다.

```bash
rm shared/art/ch-frog.jpg
```

다시 만들려면:

```bash
node tools/gen-free.mjs --only ch-frog --force --seed 99
```

`--seed` 값을 바꾸면 같은 문구로 다른 그림이 나옵니다.
